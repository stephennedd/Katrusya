import { ValidateCreateUserResultsPipe } from './validate-create-user-results.pipe';

describe('ValidateCreateUserResultsPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateUserResultsPipe()).toBeDefined();
  });
});
