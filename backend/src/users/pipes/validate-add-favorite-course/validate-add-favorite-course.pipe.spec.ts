import { ValidateAddFavoriteCoursePipe } from './validate-add-favorite-course.pipe';

describe('ValidateAddFavoriteCoursePipe', () => {
  it('should be defined', () => {
    expect(new ValidateAddFavoriteCoursePipe()).toBeDefined();
  });
});
