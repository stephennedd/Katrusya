import { ValidateCreateCategoryPipe } from './validate-create-category.pipe';

describe('ValidateCreateCategoryPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateCategoryPipe()).toBeDefined();
  });
});
