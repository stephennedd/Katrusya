import { ValidateCreateUserPurchasePipe } from './validate-create-user-purchase.pipe';

describe('ValidateCreateUserPurchasePipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateUserPurchasePipe()).toBeDefined();
  });
});
