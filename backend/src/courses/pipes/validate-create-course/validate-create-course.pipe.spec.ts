import { ValidateCreateCoursePipe } from './validate-create-course.pipe';

describe('ValidateCreateCoursePipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateCoursePipe()).toBeDefined();
  });
});
