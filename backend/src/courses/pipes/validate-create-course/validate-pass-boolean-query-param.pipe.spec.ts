import { ValidatePassBooleanQueryParam } from './validate-pass-boolean-query-param.pipe';

describe('ValidateCreateCoursePipe', () => {
  it('should be defined', () => {
    expect(new ValidatePassBooleanQueryParam()).toBeDefined();
  });
});
